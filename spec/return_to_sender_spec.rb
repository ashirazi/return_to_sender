require 'spec_helper'

describe ReturnToSender do
  it 'has a version number' do
    expect(ReturnToSender::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
